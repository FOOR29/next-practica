import { Metadata } from "next"

export const metadata: Metadata = {
    title: {
        absolute: "About"
    }
}

const AboutPage = () => {
    return (
        <div>AboutPage</div>
    )
}

export default AboutPage